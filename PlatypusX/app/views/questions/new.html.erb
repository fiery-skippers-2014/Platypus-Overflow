<h1>create a new question</h1>
	<%= form_for @question do |q| %>
		<%= q.label :title %>
		<%= q.text_field :title %><br><br>
		<%= q.label :body %>
		<%= q.text_field :body %><br><br> <%= render "shared/_vote_form" %>
		<%= q.submit "Create A Question"%>
	 <% end %>


